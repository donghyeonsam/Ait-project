# AI 면접관 이미지

`InterviewStage` 컴포넌트(`src/components/interview/InterviewStage.tsx`)가 이 폴더의 `ai-interviewer.png`를 참조합니다.

- 파일을 이 폴더에 `ai-interviewer.png`라는 이름으로 넣으면 자동으로 반영됩니다.
- 파일이 없으면 화면이 깨지는 대신 안내 placeholder(아이콘 + 문구)가 대신 표시됩니다.
- 실제 서비스용 이미지가 아니라 임시 대체 이미지이므로, 나중에 실제 AI 면접관 영상/이미지로 교체될 예정입니다.
